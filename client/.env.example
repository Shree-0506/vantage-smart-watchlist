# Copy to .env (or .env.production for a deployed build) to point the client
# at a different API. Falls back to http://localhost:4000/api if unset —
# see client/src/api.js.

VITE_API_URL=http://localhost:4000/api
