# Copy to .env if you want to override defaults. Both variables are optional —
# server/src/index.js and server/src/auth.js fall back to the values below.

PORT=4000
JWT_SECRET=dev-secret-change-in-production-groww-code-2026
